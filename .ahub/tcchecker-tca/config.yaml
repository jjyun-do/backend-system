version: 2
test:
  - name: TCM
    testCaseLanguage: KOTLIN
    testFW: JUNIT
    testCaseFolder:
      - ./account-role/src/test
      - ./account-service/src/test
      - ./data-query-service/src/test
      - ./platform/src/test

    testFile:
      - extension: kt
        ends:
          - Test
          - Tests

    testCase:
      - condition:
          - annotation:
              match:
                - '@Test'

    positiveTestCase:
      - condition:
          - annotation:
              match:
                - '@Tag("positive")'

    negativeTestCase:
      - condition:
          - annotation:
              match:
                - '@Tag("negative")'
